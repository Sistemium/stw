import Model from '@/init/Model';

export default new Model({
  collection: 'ArticleProp',
  schema: {},
  methods: {},
});

export const VALUE_TYPES = {
  string: 'string',
  options: 'options',
  boolean: 'boolean',
  number: 'number',
};
